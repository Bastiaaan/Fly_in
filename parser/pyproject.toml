
[project]
name = "fly_in-map-parser"
version = "0.1.0"
description = "Prompt simulator who seeks the right fn-function based on human language"
requires-python = ">=3.10"
dependencies = [
	"pydantic",
	"numpy",
	"mypy",
	"flake8"
]